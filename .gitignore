# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
/dist

# misc
.DS_Store
*.pem
*.local

# Raw client photo drops (web-optimized copies live in public/images)
Fotos/
Fotos /

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — .env is tracked (Web3Forms key for contact form).
# Keep machine-only overrides in .env.local (still ignored).
.env*.local
!.env
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
